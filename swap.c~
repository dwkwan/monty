#include <stdio.h>
#include "monty.h"

void swap(stack_t **stack, unsigned int line_number)
{
	int tmp;
	stack_t *new = *stack;
	stack_t *keep = 0;

	if (new == new->next)
	{
		printf("L%d: can't swap, stack too short\n", line_number);
		free_everything();
		exit(EXIT_FAILURE);

	}
}
